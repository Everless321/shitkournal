# Web PDF Fetcher for S.H.I.T Journal

## Task
Build a Vercel-deployable web application to fetch and download PDFs from shitjournal.org preprints.

## Todo
- [x] Analyze shitjournal.org PDF loading mechanism (react-pdf + Supabase Storage)
- [x] Extract Supabase project URL and publishable API key
- [x] Create CLI script `fetch_pdf.py` with detailed terminal output
- [x] Compare with existing `sync.py` from shitjournal-backup repo (no duplication)
- [x] Create `/api/info.js` — metadata + signed URL endpoint
- [x] Create `/api/download.js` — PDF proxy download endpoint
- [x] Create `public/index.html` — terminal-style frontend UI
- [x] Create `vercel.json` + `package.json` for Vercel deployment
- [x] Create `dev.mjs` for local development
- [x] Verify both API endpoints return correct data
- [x] Verify frontend UI renders correctly with full flow

## Result
All items completed. Web app tested locally — API returns correct metadata, download streams PDF with proper Content-Disposition header, frontend shows terminal-style step-by-step output with progress bar.

## Files
- `api/info.js` — GET /api/info?id=xxx, returns preprint metadata + signed URL
- `api/download.js` — GET /api/download?id=xxx, proxies PDF with Chinese title as filename
- `public/index.html` — Dark terminal-style UI with step logging
- `vercel.json` — Vercel routing config
- `package.json` — Project manifest
- `dev.mjs` — Local dev server
- `fetch_pdf.py` — CLI version (standalone)
